body {
  background-color: #f4ecd8;
  font-family: "Georgia", serif;
  color: #3e2723;
  display: flex;
  justify-content: center;
  padding: 15px 10px;
  margin: 0;
  /* РАЗРЕШАЕМ прокрутку страницы по вертикали */
  touch-action: pan-y; 
  overflow-x: hidden;
}

.game-container { 
  width: 100%; 
  max-width: 950px; 
  display: flex;
  flex-direction: column;
  align-items: center; 
}

.main-layout { 
  display: flex; 
  gap: 20px; 
  justify-content: center; 
  margin-top: 15px; 
  flex-wrap: wrap;
  width: 100%;
}

.grid {
  display: grid;
  grid-template-columns: repeat(15, 32px);
  grid-template-rows: repeat(15, 32px);
  gap: 1px;
  border: 4px double #3e2723;
  background: #3e2723;
  padding: 2px;
  margin: 0 auto;
  /* ЗАПРЕЩАЕМ прокрутку ТОЛЬКО на сетке, чтобы выделять слова */
  touch-action: none; 
}

.cell {
  width: 32px; height: 32px; background: #fffbf0;
  display: flex; align-items: center; justify-content: center;
  font-weight: bold; font-size: 16px; cursor: pointer; user-select: none;
  box-sizing: border-box;
}

@media (max-width: 600px) {
  .grid {
    grid-template-columns: repeat(15, 6vw);
    grid-template-rows: repeat(15, 6vw);
  }
  .cell {
    width: 6vw; height: 6vw;
    font-size: 3.2vw;
  }
  .main-layout { flex-direction: column; align-items: center; }
  .word-list { width: 95%; margin-top: 20px; }
}

.cell.marked { background: #d7ccc8; color: #a1887f; }
.cell.hidden { opacity: 0; pointer-events: none; transform: scale(0.8); }
.cell.answer-clue { color: #3e2723; font-family: monospace; font-size: 1.2em; background: #fdf5e6; border: 1px solid #d7ccc8; }
.cell.used-letter { opacity: 0.3; filter: grayscale(1); }
.cell.hint-glow { background: #fff59d !important; transform: scale(1.1); z-index: 10; box-shadow: 0 0 10px #fbc02d; }

.word-list { text-align: left; min-width: 280px; }
#ui-word-list { padding-left: 15px; border-left: 2px solid #d7ccc8; }
.found { text-decoration: line-through; opacity: 0.5; }

.btn-hint, .btn-new, .btn-check {
  margin: 5px; padding: 10px 15px; background: #3e2723; color: white; border: none; cursor: pointer;
}

#victory-msg { display: none; margin: 30px 0; padding: 20px; background: #efebe9; border: 1px solid #3e2723; width: 100%; box-sizing: border-box; }
.input-group { margin: 10px 0; text-align: left; }
.input-group input { width: 100%; padding: 10px; border: 1px solid #3e2723; background: transparent; box-sizing: border-box; }
/* 1. Убираем задержку клика и запрещаем системные жесты на сетке */
.grid {
    touch-action: none; /* Запрещает браузеру перехватывать касания для скролла */
    -webkit-tap-highlight-color: transparent; /* Убирает синюю рамку при нажатии на iOS */
}

/* 2. Запрещаем выделение текста (чтобы вместо клика не открывалось меню копирования) */
.cell {
    user-select: none;
    -webkit-user-select: none;
    -ms-user-select: none;
}

/* 3. Улучшаем визуальный отклик для финальной фазы */
.cell.answer-clue {
    cursor: pointer;
    transition: background-color 0.2s, opacity 0.2s;
}

/* 4. Стиль для "закрашенной" буквы (сделайте его ярче, чтобы видеть результат) */
.cell.used-letter {
    background-color: #3e2723 !important; /* Темный фон */
    color: #ffffff !important;           /* Белая буква */
    opacity: 0.6;
    text-decoration: line-through;       /* Зачеркивание для верности */
}
